import ReactDOM from "react-dom/client";
import { ServerConnection } from "./components/connectServer";
import React from "react";

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <ServerConnection />
)
